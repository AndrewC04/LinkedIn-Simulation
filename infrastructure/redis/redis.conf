# Redis config for LinkedIn sim
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence: save a snapshot every 60s if at least 1 key changed
save 60 1

# Basic security
requirepass redispassword

# Allow connections from any IP inside Docker network
bind 0.0.0.0
protected-mode no