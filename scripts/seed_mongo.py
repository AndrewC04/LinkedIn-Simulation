# MongoDB analytics events are generated naturally through user interactions via Kafka pipeline
# No fake seed data - all events come from real usage
print("MongoDB: no seed data needed - events generated through Kafka pipeline")
